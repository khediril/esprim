{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie.name }}</td>
            </tr>
        </tbody>
    </table>
    <hr>
    <table class="table">
    {% for p in categorie.produits %}
            <tr>
                <td>{{ p.id }}</td>
                <td>{{ p.name }}</td>
                
            </tr>
        {% else %}
            <tr>
                <td colspan="3">aucun produit qui appartient à cette categorie</td>
            </tr>
        {% endfor %}
        </table>


    <hr>

    <a href="{{ path('categorie_index') }}">back to list</a>

    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}">edit</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
